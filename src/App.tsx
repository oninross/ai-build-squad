export function App() {
  return (
    <main className="app-shell">
      <h1>AI Build Squad Scaffold</h1>
      <p>
        React + TypeScript + SCSS + Vitest + Playwright + Storybook are
        configured.
      </p>
    </main>
  );
}
